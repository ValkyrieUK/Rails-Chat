    <div class="window">
      <div class="profile-card">
        <%= image_tag avatar_url(@user.email), :class => 'card-image' %>
        <h1>Username</h1>
        <p><%= @user.username %></p>
        <h1>Email</h1>
        <p><%= @user.email %></p>
      </div>
    </div>
